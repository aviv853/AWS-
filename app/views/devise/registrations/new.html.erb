<div class="container home-sign_up-links">
  <div class="row">
    <div class="col-md-offset-3 col-md-6">
      <% if params[:plan] == '2' %>
        <%= render "pro_form" %>
      <% else %>
        <%= render "basic_form" %>
      <% end %>
    </div>
  </div>
</div>